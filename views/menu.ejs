<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menu - <% RESTAURANT.name %></title>
</head>
<body>
  <h1>Full Menu - <%= RESTAURANT.name %></h1>
  <nav>
    <a href="/">Home</a>
    <a href="/menu">Full Menu</a>
  </nav>
  <% if(isOpen()) { %>
    <p>Yes we are open!</p>
  <% } else { %>
    <p>Sorry, we are closed.</p>

 <% } %>
 <h2>Menu</h2>
 <ul>
    <% menu.forEach(item => { %>
        <li>
            <h3><%= item.name %> - $<%= item.price %></h3>
            <p><%= item.details %></p>
            <p>Rating: <%= item.rating %> / 5</p>
        </li>
 <%}); %>
 <% include('partials/nav') %>
 </ul>
</body>
</html>
